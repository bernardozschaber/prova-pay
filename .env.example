# Copy to .env and adjust. Without DATABASE_URL the app uses SQLite (db.sqlite3).
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=1
# DATABASE_URL=postgres://provapay:provapay@localhost:5432/provapay
ALLOWED_HOSTS=localhost,127.0.0.1
